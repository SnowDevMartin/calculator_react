const ButtonEqual = ({ children }) => {

  return (
    <button
      className="flex items-center buttonequal text-white justify-center rounded-full w-full h-full row-span-2 text-3xl"
    >
      {children}
    </button>
  );
};

export default ButtonEqual;
